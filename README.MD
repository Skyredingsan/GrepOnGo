# GrepOnGo - аналог grep на Go

![Go Version](https://img.shields.io/badge/Go-1.19+-00ADD8?logo=go)
![License](https://img.shields.io/badge/License-MIT-blue)
![Status](https://img.shields.io/badge/Status-Production-green)

**GrepOnGo** - это мощная утилита для поиска текста в файлах, написанная на Go. Поддерживает все основные функции классического `grep`.


## 📦 Установка

### Способ 1: Сборка из исходников
```bash
# Клонируйте репозиторий
git clone https://github.com/Skyredingsan/GrepOnGo.git
cd gogrep

# Соберите программу
go build -o GrepOnGo main.go

# Добавьте в PATH (опционально)
sudo mv GrepOnGo /usr/local/bin/
```

### Способ 2: Установка через go install
```bash
go install github.com/Skyredingsan/GrepOnGo@latest
```

## 🚀 Использование

### Базовый синтаксис
```bash
gogrep -e "паттерн" -f файл1,файл2 [опции]
```

### Параметры командной строки

| Флаг | Описание | Пример |
|------|----------|--------|
| `-e` | Паттерн для поиска (обязательный) | `-e "error"` |
| `-f` | Список файлов через запятую | `-f "file1.txt,file2.txt"` |
| `-i` | Игнорировать регистр | `-i` |
| `-n` | Показать номера строк | `-n` |
| `-c` | Только подсчет совпадений | `-c` |
| `-v` | Инвертировать поиск | `-v` |
| `-r` | Использовать регулярные выражения | `-r` |
| `-H` | Подсветка совпадений | `-H` |
| `-A N` | Показать N строк после совпадения | `-A 3` |
| `-B N` | Показать N строк перед совпадением | `-B 2` |
| `-C N` | Показать N строк вокруг совпадения | `-C 2` |

